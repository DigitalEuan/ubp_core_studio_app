
export const INITIAL_SYSTEM_KB = ``;

export const INITIAL_STUDY_KB = `# UBP Study Knowledge Base
## Active Study: [Using the UBP]

- Never make placeholder or fake or simplified work here, all work must be full and complete. Do not use floats in calculations.
`;

export const INITIAL_HASH_MEMORY_KB = `[]`;

export const INITIAL_BELIEFS_KB = `[]`;

// Files are now fetched from GitHub. Keeping const for reference but empty default.
export const INITIAL_AUTO_TRIGGER_SCRIPT = `"""
# Placeholder. 
# The actual auto_trigger.py is now fetched from GitHub 
# (DigitalEuan/UBP_Repo/main/core_studio_v4.0/core/auto_trigger.py)
"""`;
